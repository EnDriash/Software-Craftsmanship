import { expect } from 'chai';
// import paginateArray from '../JS/pagination.ts';
// console.log(paginateArray);

describe('PaginateArray', () => {
    it('Should be a function', () => {
        expect(true).to.be.a('boolean');
    });
});
